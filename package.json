{
  "name": "todolist",
  "version": "1.0.0",
  "description": "A simple To-Do List application with TailwindCSS",
  "main": "index.js",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "tailwindcss": "^3.4.17"
  }
}
